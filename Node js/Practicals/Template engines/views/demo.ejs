// demo.ejs File
<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title></title>
	</head>
	<body>
		<% if (user) { %>
		<h2><%= user.name %></h2>
		<% } %> <% users.forEach(element => { %> <%= element.name %> <% }); %>
	</body>
</html>
